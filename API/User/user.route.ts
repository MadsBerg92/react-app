// import express from "express";

// import * as userContr from "./user.controller";
// export const userRoute = express.Router();

// // middleware specific to this route
// userRoute.use(express.json());

// // route handlers
// userRoute.post("/user", userContr.postUser);

// userRoute.get("/user/:email", userContr.getUser);

// userRoute.put("/user/:email", userContr.putUser);

// userRoute.get("/user/:email/orders", userContr.getOrders);
